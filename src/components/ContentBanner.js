import React from 'react';
import PropTypes from 'prop-types';

const ContentBanner = ({ image, title }) => (
  <section className="featuredBlock-wrapper"></section>
);

ContentBanner.propTypes = {};

export default ContentBanner;
